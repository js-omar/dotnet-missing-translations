// eslint-disable-next-line no-shadow
enum FileTypesEnum {
  Shared = 'shared',
  Models = 'models',
}

const FileTypes = [FileTypesEnum.Shared, FileTypesEnum.Models];

export { FileTypesEnum, FileTypes };
